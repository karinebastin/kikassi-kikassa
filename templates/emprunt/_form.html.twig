{{ form_start(form) }}
<div class="form-group">
	{{ form_widget(form) }}
	<button class="btn btn-dark bg-green w-100">{{ button_label|default('Réserver') }}</button>
</div>
{{ form_end(form) }}

{# app.user #}
{# objet #}
