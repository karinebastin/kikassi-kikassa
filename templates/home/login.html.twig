{% extends 'base.html.twig' %}

{% block title %}Connexion
{% endblock %}

{% block body %}
	{% include "/partials/header.html.twig" %}
	<div class="container">
		<div class="row">
			<div class="col-12 margin-top">
				<form
					method="post" class="m-5">
					{# {% if error %}
										<div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
									{% endif %} #}

					{% if app.user %}
						<div class="mb-3">
							You are logged in as
							{{ app.user.username }},
							<a href="{{ path('app_logout') }}">Logout</a>
						</div>
					{% endif %}

					<h1 class="h3 font-weight-normal my-5 text-center">Connectez-vous</h1>
					<label for="inputEmail">Email</label>
					<input type="email" id="inputEmail" class="form-control" required autofocus>
					<label for="inputPassword">Password</label>
					<input type="password" name="password" id="inputPassword" class="form-control" required>

					<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

					<button class="btn btn-lg btn-dark mt-4 form-control bg-green" type="submit">
						Connexion
					</button>
				</form>
			</div>
			<div class="col-12 text-center my-2">
				<p>Si vous n'avez pas encore de compte, merci de prendre contact avec l'association pour sa création.</p>
			</div>
			<div class="text-center w-100 my-4">
				<a class="btn btn-lg btn-dark bg-green" href="{{ path('home') }}">retour</a>
			</div>
		</div>
	</div>
	{% include "/partials/footer.html.twig" %}
{% endblock %}
